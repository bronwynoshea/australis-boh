# Legacy Edge Functions To Delete

These folders are old unprefixed SLOTZ Edge Function slugs that should not be used going forward.

The active deployable functions live in `supabase/functions` and use the `slotz-` prefix.

After BOH-dev smoke tests pass, delete the matching old remote Supabase Edge Functions from the dashboard or CLI.
