import type { BohUser, BohInvite, User } from './types';

// App paths mapping
export const APP_PATHS: Record<string, string> = {
  counter: '/counter',
  careerStudio: '/boh-career-studio/',
  patron: '/patron/dashboard',
  talent: '/boh-talent/',
  tablez: '/apps/tablez',
  cookbook: '/cookbook',
  chatz: '/apps/chatz',
};

