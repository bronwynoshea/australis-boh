export interface RecipeDefinition {
  id: string;
  name: string;
  category: string;
  description: string;
}

export interface RecipeTemplateForm {
  slug: string;
  headline: string;
  body: string;
}
